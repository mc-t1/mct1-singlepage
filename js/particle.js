/*
        selector: '#myCanvas1',
        color: '#0f9976',
        connectParticles: false,
        sizeVariations: 5,
        speed: 0.5,
        maxParticles: 100,
*/

function startBGL(options) {
    Particles.init( options );
};

function stopBGL() {
    Particles.stop();
}
